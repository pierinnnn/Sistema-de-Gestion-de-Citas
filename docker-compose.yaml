version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=mongodb://mongo:27017/appointments
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"